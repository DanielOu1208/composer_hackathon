# Required: master password for vault encryption (hackathon: set in env only)
VAULT_MASTER_PASSWORD=your-secure-master-password

# Optional: for github_create_issue demo - store GITHUB_TOKEN in Vault via UI instead,
# or set here for dev (gateway will use it if not in vault)
# GITHUB_TOKEN=ghp_xxxx
